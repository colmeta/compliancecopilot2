Flask==2.3.3
Flask-CORS==4.0.0
python-docx==0.8.11
PyPDF2==3.0.1
gunicorn==21.2.0
requests==2.32.5 
google-generativeai
Werkzeug
python-magic
# We no longer need google-generativeai, but it's okay to leave it for now.
